apiVersion: v1
kind: Service
metadata:
  name: hotel-booking-system
  namespace: hotel-booking
  labels:
    app: hotel-booking-system
spec:
  selector:
    app: hotel-booking-system
    version: blue
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  type: LoadBalancer